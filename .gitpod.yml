tasks:
  - name: Install
    init: |
      export HMR_HOST=`gp url 3000`
      ./install.sh
  - command: echo Done installing! Now press CTRL + SHIFT + B
ports:
  - port: 4000
    onOpen: notify
  - port: 30000
    onOpen: ignore

vscode:
  extensions:
    - svelte.svelte-vscode
    - ms-python.python
