{
	"private": true,
	"scripts": {
		"lint": "prettier --check . && (cd client && eslint .) && (cd server && eslint .) && (cd typings && eslint .)",
		"format": "prettier --write ."
	},
	"workspaces": [
		"client",
		"server",
		"@landstown/typings"
	],
	"packageManager": "yarn@3.2.3",
	"devDependencies": {
		"prettier": "^2.7.1",
		"prettier-plugin-svelte": "^2.7.0",
		"svelte": "^3.50.1"
	}
}
